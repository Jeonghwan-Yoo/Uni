//0.5에 대한 아크 싸인 값을 구하는 프로그램을 작성하세요.
#include <stdio.h>
#include <math.h>

void main(void) {
	double x;

	x = asin(0.5);

	printf("asin(0.5) : %g\n", x);
}
//double asin(double x);
//x=acos(0.5), x=atan(0.5)